# Global Instructions

## Markdown Formatting

When generating Markdown (files, commit messages, PR descriptions, comments, etc.), always include blank lines before and after headers, per the Markdown specification.

## Text Files

Every text file you create or edit must end with a newline character. This includes files written by scripts or generated from another tool's output; if a library strips the final newline, add it back before writing.
